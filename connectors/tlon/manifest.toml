[manifest]
format = "fcp-connector-manifest"
schema_version = "2.1"
min_mesh_version = "2.0.0"
min_protocol = "fcp2-sym/2.0"
protocol_features = []
max_datagram_bytes = 65000
interface_hash = "blake3-256:fcp.interface.v2:5259bbb5dac0c306c2d64e1a15276345e0f1cd1efcace4a0d6c32dcbb1152c28"

[connector]
id = "fcp.tlon"
name = "Tlon"
version = "0.1.0"
status = "proven"
description = "Tlon / Urbit connector for authenticated DM and channel send flows"
archetypes = ["bidirectional"]
format = "native"

[connector.state]
model = "stateless"
state_schema_version = "1"
migration_hint = "First slice: authenticated DM and channel send flows with explicit SSRF-safe base URL validation."

[zones]
home = "z:community"
allowed_sources = ["z:owner", "z:work", "z:community"]
allowed_targets = ["z:community"]

[capabilities]
required = ["network.dns", "network.egress", "network.tls.sni", "tlon.dm", "tlon.channel"]
optional = []
forbidden = ["network.listen", "system.exec"]

[sandbox]
profile = "strict"
memory_mb = 96
cpu_percent = 25
wall_clock_timeout_ms = 60000
deny_exec = true
deny_ptrace = true

[provides.operations."tlon.dm.send"]
description = "Send a direct message to a ship on the Tlon network"
capability = "tlon.dm"
risk_level = "medium"
safety_tier = "safe"
requires_approval = "none"
idempotency = "best_effort"

[provides.operations."tlon.dm.send".network_constraints]
host_allow = ["${tlon_base_host}"]
port_allow = [80, 443]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = false
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 60000
max_response_bytes = 1048576

[provides.operations."tlon.dm.send".input_schema]
required = ["ship", "message"]
type = "object"
additionalProperties = false

[provides.operations."tlon.dm.send".input_schema.properties.ship]
description = "Target ship name (e.g. ~zod)"
type = "string"

[provides.operations."tlon.dm.send".input_schema.properties.message]
description = "Message text to send"
type = "string"

[provides.operations."tlon.dm.send".output_schema]
required = ["ok"]
type = "object"
additionalProperties = false

[provides.operations."tlon.dm.send".output_schema.properties.ok]
type = "boolean"

[provides.operations."tlon.dm.send".output_schema.properties.coordination]
description = "Redacted chat coordination audit records"
type = "array"

[provides.operations."tlon.dm.send".ai_hints]
when_to_use = "When you need to send a direct message to a ship on the Tlon/Urbit network."
common_mistakes = ["Omitting the ~ prefix on ship names."]
examples = []
related = []

[provides.operations."tlon.channel.send"]
description = "Send a channel message on the Tlon network"
capability = "tlon.channel"
risk_level = "medium"
safety_tier = "safe"
requires_approval = "none"
idempotency = "best_effort"

[provides.operations."tlon.channel.send".network_constraints]
host_allow = ["${tlon_base_host}"]
port_allow = [80, 443]
ip_allow = []
cidr_deny = []
deny_localhost = false
deny_private_ranges = false
deny_tailnet_ranges = false
require_sni = false
spki_pins = []
deny_ip_literals = false
require_host_canonicalization = true
dns_max_ips = 16
max_redirects = 0
connect_timeout_ms = 10000
total_timeout_ms = 60000
max_response_bytes = 1048576

[provides.operations."tlon.channel.send".input_schema]
required = ["channel", "message"]
type = "object"
additionalProperties = false

[provides.operations."tlon.channel.send".input_schema.properties.channel]
description = "Target channel path or identifier"
type = "string"

[provides.operations."tlon.channel.send".input_schema.properties.message]
description = "Message text to send"
type = "string"

[provides.operations."tlon.channel.send".output_schema]
required = ["ok"]
type = "object"
additionalProperties = false

[provides.operations."tlon.channel.send".output_schema.properties.ok]
type = "boolean"

[provides.operations."tlon.channel.send".output_schema.properties.coordination]
description = "Redacted chat coordination audit records"
type = "array"

[provides.operations."tlon.channel.send".ai_hints]
when_to_use = "When you need to send a message into a Tlon/Urbit channel."
common_mistakes = ["Using a DM target where a channel path is required."]
examples = []
related = []

[provides.operations."tlon.target.resolve"]
description = "Resolve a Tlon DM or channel target before sending"
capability = "tlon.channel"
risk_level = "low"
safety_tier = "safe"
requires_approval = "none"
idempotency = "strict"

[provides.operations."tlon.target.resolve".network_constraints]
host_allow = ["none.invalid"]
port_allow = [0]
ip_allow = []
cidr_deny = []
deny_localhost = true
deny_private_ranges = true
deny_tailnet_ranges = true
require_sni = false
spki_pins = []
deny_ip_literals = true
require_host_canonicalization = true
dns_max_ips = 0
max_redirects = 0
connect_timeout_ms = 1000
total_timeout_ms = 10000
max_response_bytes = 65536

[provides.operations."tlon.target.resolve".input_schema]
required = ["target"]
type = "object"
additionalProperties = false

[provides.operations."tlon.target.resolve".input_schema.properties.target]
description = "Human-friendly DM or channel target to resolve"
type = "string"

[provides.operations."tlon.target.resolve".output_schema]
required = ["resolved"]
type = "object"
additionalProperties = false

[provides.operations."tlon.target.resolve".output_schema.properties.resolved]
type = "boolean"

[provides.operations."tlon.target.resolve".ai_hints]
when_to_use = "When you need to normalize or validate a Tlon target before sending."
common_mistakes = []
examples = []
related = []
